using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ControlSystem : MonoBehaviour
{
    /*private static ControlSystem controlSystem = null;
    private ControlSystem GetInstance()
    {
        return controlSystem;
    }
    void Start()
    {
        num_pad = new List<bool>();
        for (int i = 0; i < 10; i++) num_pad.Add(false); // 10 teclas numericas

        controlSystem = this;
    }

    private bool key_w = false, key_a = false, key_s = false, key_d = false,
                 key_tab = false, key_backspace = false,
                 mouse_left = false, mouse_right = false;

    private List<bool> num_pad = null;


    void Update()
    {
        
    }*/
}
